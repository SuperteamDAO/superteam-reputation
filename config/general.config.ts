import { ConfigInterface } from '../src/interfaces/config';

const config: ConfigInterface = {
  general: {
    name: 'Superteam Reputation',
    domain: 'https://reputation.superteam.fun/',
    twitterHandle: '@SuperteamDAO',
  },
};

export default config;
